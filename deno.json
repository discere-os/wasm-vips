{
  "name": "@discere-os/vips.wasm",
  "version": "0.0.14",
  "description": "High-performance libvips image processing library with WebAssembly, SIMD optimizations, and TypeScript-first API",
  "exports": {
    ".": "./src/lib/index.ts",
    "./types": "./src/lib/types.ts",
    "./vips.wasm": "./lib/vips.wasm",
    "./vips-heif.wasm": "./lib/vips-heif.wasm",
    "./vips-jxl.wasm": "./lib/vips-jxl.wasm",
    "./vips-resvg.wasm": "./lib/vips-resvg.wasm"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.14",
    "@std/testing": "jsr:@std/testing@^1.0.0",
    "@std/encoding": "jsr:@std/encoding@^1.0.5",
    "@std/streams": "jsr:@std/streams@^1.0.6",
    "module": "data:application/javascript,export const createRequire = () => (id) => { if (id === 'path') return { dirname: () => '', join: (...args) => args.join('/') }; if (id === 'url') return { fileURLToPath: (url) => url.replace('file://', '') }; if (id === 'fs') return { readFileSync: () => { throw new Error('fs not supported'); } }; throw new Error(`Module ${id} not found`); }; export default { createRequire };",
    "path": "data:application/javascript,export const dirname = () => ''; export const join = (...args) => args.join('/'); export default { dirname, join };",
    "url": "data:application/javascript,export const fileURLToPath = (url) => url.replace('file://', ''); export default { fileURLToPath };",
    "fs": "data:application/javascript,export const readFileSync = () => { throw new Error('fs not supported'); }; export default { readFileSync };",
    "util": "data:application/javascript,export const promisify = fn => (...args) => new Promise((resolve, reject) => fn(...args, (err, result) => err ? reject(err) : resolve(result))); export default { promisify };"
  },
  "tasks": {
    "demo": "deno run --allow-read --allow-write demo-deno.ts",
    "demo:resize": "deno run --allow-read --allow-write demo-resize.ts",
    "demo:effects": "deno run --allow-read --allow-write demo-effects.ts",
    "demo:formats": "deno run --allow-read --allow-write demo-formats.ts",
    "test": "deno test --allow-read --allow-write --no-check tests/deno/",
    "test:basic": "deno test --allow-read --allow-write --no-check tests/deno/basic.test.ts",
    "test:formats": "deno test --allow-read --allow-write --no-check tests/deno/formats.test.ts",
    "test:operations": "deno test --allow-read --allow-write --no-check tests/deno/operations.test.ts",
    "benchmark": "deno run --allow-read --allow-write bench/vips.bench.ts",
    "benchmark:resize": "deno run --allow-read --allow-write bench/resize.bench.ts",
    "benchmark:filters": "deno run --allow-read --allow-write bench/filters.bench.ts",
    "build": "deno task build:docker",
    "build:docker": "docker build -t wasm-vips . && docker run -it --rm -v $(pwd):/src wasm-vips ./build.sh",
    "build:npm": "deno run --allow-all _build_npm.ts",
    "build:all": "deno task build:docker && deno task build:npm",
    "publish:npm": "deno task build:all && cd npm && npm publish",
    "publish:dry": "deno task build:all && cd npm && npm publish --dry-run",
    "serve": "deno run --allow-all --allow-net=:8000 scripts/serve.ts",
    "clean": "rm -rf lib/*.wasm lib/*.js dist/ npm/ && echo '✅ Cleaned all build artifacts'",
    "check": "deno check src/lib/index.ts",
    "check:all": "deno check src/lib/index.ts && deno check demo-deno.ts && deno check bench/vips.bench.ts"
  },
  "compilerOptions": {
    "lib": ["deno.ns", "dom", "es2022", "deno.unstable"],
    "strict": true,
    "allowUnreachableCode": false,
    "allowUnusedLabels": false,
    "exactOptionalPropertyTypes": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noPropertyAccessFromIndexSignature": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  "fmt": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["lib/", "build/", "npm/", "src/closure-externs/", "src/modules-pre.js", "src/workaround-cors-pre.js"]
  },
  "lint": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["lib/", "build/", "npm/", "src/closure-externs/", "src/modules-pre.js", "src/workaround-cors-pre.js"]
  },
  "test": {
    "include": ["tests/deno/"],
    "exclude": ["test/unit/", "test/bench/"]
  }
}